# Paper Plugin

Build:

```bash
mvn clean package
```

Output jar:

- `target/minecraft-jira-plugin.jar`

Install jar in Paper `plugins/` and configure:

- `plugins/MinecraftJira/config.yml`
  - `backend.base-url`
  - `backend.api-key`
