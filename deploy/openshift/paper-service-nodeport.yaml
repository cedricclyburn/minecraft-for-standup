apiVersion: v1
kind: Service
metadata:
  name: minecraft-paper
  namespace: minecraft
spec:
  type: NodePort
  selector:
    app: minecraft-paper
  ports:
  - name: minecraft
    port: 25565
    targetPort: 25565
    nodePort: 32565
